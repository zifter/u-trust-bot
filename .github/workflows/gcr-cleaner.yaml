name: gcr-cleaner

on:
  schedule:
    - cron: '0 0 */1 * *' # runs daily
  workflow_dispatch: # allows for manual invocation

jobs:
  gcr-cleaner:
    runs-on: 'ubuntu-latest'
    steps:
      - name: 'Authenticate to Google Cloud'
        uses: 'google-github-actions/auth@v1'
        with:
          credentials_json: '${{ secrets.GOOGLE_CREDENTIALS }}'

      - name: 'Authenticate to Docker'
        run: |
          gcloud auth print-access-token | docker login \
          -u oauth2accesstoken \
          --password-stdin https://europe-central2-docker.pkg.dev

      - name: clean-e2e
        uses: 'docker://europe-docker.pkg.dev/gcr-cleaner/gcr-cleaner/gcr-cleaner'
        env:
          GCRCLEANER_LOG: debug
        with:
          args: >-
            -repo=${{ secrets.GCP_ARTIFACT_REGISTRY }}/bot/e2e
            -recursive
            -grace=1h
